# Continue on source height and shared-future maps

The proved full-system reduction is in note.md Sections 4-9. The original integral
first-jet kernel remains; the relevant filtration is by original generator height,
not by the size of every intermediate path vertex. A class reduced to a lower subgroup
is not thereby zero. Both counterexample component types are retained by the actual
chain-homotopy equivalence.

The sharp first-crossing envelope works at every depth without transcendence input.
Its *verified source window* currently covers crossings with at most 10,280 odd steps,
for all positive starts. It reduces to the complete source interval through 5,624,777,
whose actual first crossings all occur by 141 odd returns. The first next envelope
record is (m,A,H)=(10281,16295,6728242). A further horizon requires the corresponding
complete source checks, or another source argument; do not extrapolate this receipt.
Terras's CST conjecture is the existing unrestricted first-crossing question.

The exact generic family engine `coalescence_family(left,right)` retains both ordered
words, all coefficients, original target-image moduli, compatible target progression,
the two original source progressions, inverse parameters, and the strict height domain.
It also retains incompatible and empty-height labels. Nonempty finite source cylinders
are not a common ordinary-integer source for an infinite branch.

The first new family handles 1731+2916t by its smaller common-future source 1215+2048t.
Its left sources are divisible by 3, hence cannot be handled as direct forward images
of some other integer. The original paths synchronize after 1 and 7 returns, with
signed first-jet clock -6. Reuse the exact source maps rather than declaring a forward
trajectory n -> smaller_source. The old/new retraction identities permit further
such rules to attach at old roots with all integral source chains retained.

Next useful calculation: extend complete shared-future source families on the retained
roots, especially the classes divisible by 3, and preserve priority of the current
rules to obtain exact extension maps. The current root set (apart from 1) is
n mod36 in {3,7,15,19,27}, excluding 8731 mod8748 and 1731 mod2916. A finite complete
rule set that sends every original source to 1 has not been constructed. Residue
counts of these selected roots are not counterexample probabilities.

A nonbase component minimum must exceed 5,624,777, survive that precise root set, and
have no first crossing in the certified horizon. Global coverage of higher-source
roots and ordinary-integer support of infinite noncrossing paths remain. A proposed
infinite obstruction must be an original supported source, not a timeout or open finite
frontier. Finite integral first-jet witnesses can be extracted and checked with the
unchanged published receiver.

This directory is a local checked additive increment until a separate remote receipt
states otherwise. Main and the old history-law branch are not touched by local work.
