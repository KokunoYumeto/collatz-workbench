# Continuation state — 14 September 2026

## Actual target

Push the original positive-integer Collatz problem through the preserved source maps, not just through a statistic that loses counterexamples. The current exact object is the relative actual-orbit complex and its compatible whole-source cutoff retractions.

Theorem 6.2 proves that every original source column has an eventually stable, finite-support descent path to a residual root. That root is either 1 or an original never-reference-certified integer. It does not identify all residual roots as 1. A surviving periodic cycle remains the same original edge chain; a nonperiodic component remains in degree-zero component homology and invariant degree-zero cohomology.

## Proved and implemented in this continuation

The joint source/image incidence complex maps to the predecessor's image complex and to the return complex with the literal forcing identity `C = D*r + 2*U*k`. The closed-cycle integral primitive and positivity give exact cycle membership. The map to the actual relative graph is defined from that primitive, with repetitions retained as an explicit covering map.

Checked descent paths produce `(F,Q,H)` with `boundary*H = I-Q`, `F = I-H*boundary`, and `F(c)=c` for every closed chain. The actual predecessor observer supplies an infinite-family version whose queries terminate by strict descent. Raising the exponent cutoff gives compatible commuting retractions. Their pointwise limit localizes the complex to the original never-certified source without erasing it.

The all-1 infinite code is excluded from ordinary positive support by its exact residue tower, while every finite all-1 prefix remains present. Primitive cycle words with at most one exponent 1 are excluded by a complete arbitrary-length argument. This small exclusion is not advertised as exceeding established cycle bounds.

## Immediate mathematical calculation

Operate on the retained residual roots and the projected original edges `F_H(e_n)`, rather than restarting uniformly on an artificial interval. Add an actual certified descent family or an exact arithmetic obstruction, then apply the proved extension identities. Each proposed extension must retain the source chart, target step, affine offset, first-descent mark, and boundary chain.

For cycles, an integer positive solution of the original forcing equation is converted into an actual relative cycle chain and a period pairing. A candidate found by the detector needs its complete original word and independent replay; the control forcing `-1` cannot be substituted.

For a nonperiodic survivor, retain the nondecreasing canonical source-residue sequence and the no-descent inequalities at an actual component minimum. Positive integer support is exactly eventual stabilization of that canonical residue sequence. This is the precise unresolved source calculation; zero reference mass does not remove the corresponding basis element.

The full proof object is a source-complete nonnegative integral primitive of the unit edge cochain, or a finite-support contraction column for every original source. No such global construction or original positive counterexample is claimed here.

## Integration state and ownership

The preceding ten files are kept byte-for-byte. New material is additive in this sibling directory, plus a new routing note and a read-only replay workflow. The source package was replayed in ordinary and optimized Python. A current remote base, current methodology file, claimed earlier PR, and unpushed local Codex state remain unread.

The attached local Git bundle/patch is a packaging and handoff mechanism. Integrate only after inspecting the current workbench methodology and conventions in an authenticated checkout. The supplied integration tool rejects differing destination files and never writes the Zeta repository. No current remote branch, PR, commit, or CI status is assigned to this handoff.
