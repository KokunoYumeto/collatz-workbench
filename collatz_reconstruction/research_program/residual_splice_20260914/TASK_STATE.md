# Current task state — residual-source continuation, 14 September 2026

## Target and entry point

Continue the original positive-integer Collatz problem through actual source maps. Read `README.md`, `note.md`, and `integral_blocks.md` in this directory. The inherited checkpoint is `32b8f3b9cc6dcfc4387ff783c4f4a6f6a365f717`; the later publication head belongs to the GitHub PR/issue receipt. This file records mathematical work, not a scheduled agent or background process.

## Completed in this contribution

First-descent cells retain their complete original parameter fibers. Unbounded last-exponent tails retain a single source progression together with the valuation map back to every original exponent. `split_prefix` gives a finite complete next-step decomposition into noncontracting children, crossing-child descent cells with their finite exceptional source boxes, and one entire certified tail. No exponent-overflow label is needed at that step; the infinite-depth frontier is still present.

The new homotopy is attached only at old residual roots. Each root block goes to an actual lower integer and then follows the old endpoint path. The correction lies in the old projected edge module, and the new and old projections compose to the new projection in both orders. This preserves actual cycle chains and degree-zero component obstructions. The 27 block discharges its cutoff-64 root without changing that cutoff. The entire family `(4^j-1)/3`, `j>=33`, is also discharged at that cutoff using its retained actual exponent `2j`.

The integral block matrix is chain equivalent over the integers to the full original cyclic equations. The inverse of the forced equation is `x=S0*z+H*b`, not `S0*z`. The prime-power control and its failed lift survive exactly. Splitting at exponent-1 letters retains a gap tuple, rotation offset, full original word, and actual graph edge expansion.

The executed cycle feedback proves all odd sources at most 99,779 reach 1, then excludes every nontrivial positive cycle with at most 402 exponent-1 letters at all lengths. This is a self-contained workbench bound, not a record claim. The original 21 frozen payload files remain unchanged.

## Next finite arithmetic sector

The 403-exponent-1 sector is forced to `(m,A)=(971,1539)` with cycle minimum in `[99781,330749]`. The original word then contains exactly 403 ones and 568 twos. Cut at ones, retaining the rotation offset: the 403 gap lengths `r_i>=0` sum to 568, and the original anchored equations are

```
2*4^r_i*y_(i+1) - 3^(r_i+1)*y_i = 2*3^r_i.
```

The full comparison maps in `integral_blocks.md` transfer this matrix, its forcing, and all prime-power solution sets back to the 971 original equations. The sector has not been exhausted. Two exact continuations are available: extend the certified minimum range through the complete interval using actual descent paths, or prove a forcing/positivity obstruction on every retained gap tuple. Do not infer that a modular solution or a zero integral forcing class supplies a nontrivial positive orbit; reconstruct all original coordinates and valuations and compute its actual relative cycle chain.

## Original infinite source

An actual non-base component minimum avoids every occupied first-descent cell and whole tail. Each of its prefixes is covered by the exact next-step decomposition. A first coefficient crossing leaves the explicitly retained finite source box; no crossing leaves the noncontracting-prefix frontier. In either case ordinary integer support requires stabilization of the same canonical source-residue tower. The remaining task is to eliminate these actual integer-supported residual sources or produce one with a rigorously certified infinite behavior. A measure-zero statement does not eliminate a vertex.

The source-complete nonnegative integral primitive of the unit edge cochain remains unconstructed. No nontrivial positive cycle or certified divergent positive orbit was obtained here. Continue from the exact residual fibers, rather than treating the larger collection of finite residue paths as an integer counterexample.

## Replay and evidence

From the repository root:

```
python -B collatz_reconstruction/research_program/residual_splice_20260914/replay.py
```

The verifier retains the four closed period windows, the next unexcluded one, original example words and charts, finite exceptional-source scan, new/old homotopy checks, prime-power control, rejected malformed inputs, and ordinary/optimized byte comparison. General formulas are proved in the two notes. No Lean execution or independent external mathematical review is claimed.
