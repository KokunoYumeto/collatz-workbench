# Continuation state: 16 September 2026

## Current usable additions

Read `note.md`, Theorems 2 and 3. The proved gap inequalities for `(U,L,K)=(8,9,4)` and `(2048,2187,46)` dominate the exact change of integral section from a negative signed-cycle anchor to the positive fixed point. Every positive source in the repeated-shadow/falling-tail language descends at its actual coefficient crossing. All positive affine offsets, lattice steps, parameter inverses, and original valuations are retained.

The nine signed anchor packets are exact `+1`-forcing data, not positive counterexamples or a replacement map. The source span is the original position interval, evaluated into both signed and positive graphs. The positive evaluation is not factored through a closed negative cycle.

The first-jet equation for an accepted path is an explicit equality between original infinitesimal source classes. The new endpoint is smaller. Individual endpoint witnesses are checked by the unchanged predecessor. The symbolic statement does not itself certify all smaller endpoints globally.

## Residual mixed switching

At fixed shadow repetition r, only the original finite falling-tail language with `U^r*2^B < L^r*3^s` remains. Its full length/sum counts and parameter cylinders are computed; an exponent-one exit is retained. For the two-letter packet the coarse residue count terminates before r/2 falling steps; for the seven-letter packet before r/4, with ceilings as proved in the note. There is no positive coefficient-crossing exception box in this language.

The exact reset strata after `(1,2)^r,3` are now available. On `t==1 mod 8`, the next rising run has `k=nu_2(9^r*t-1)-3`, with its inverse residue formula. A fixed r permits arbitrarily large k through the original t parameter, so a rank depending only on r cannot be declared decreasing. On the retained t=1 ladder, k equals nu_2(r). The remaining parameter and the new original endpoint must both be retained in any proposed rank.

The r=12,t=1 exit was continued to a 34-return first descent on its complete original cylinder. It is a specific proved family, not an assertion about all r or all switches. Continue developing such complete switched families or an arithmetic argument controlling the full `(r,t)` support, rather than replacing t by a statistical model.

## Cycle and nonperiodic scope

All positive cycles in the new repeated-packet/falling-tail language are excluded at arbitrary lengths. The original `(1,2)^1507,(2)^617` ordering of the earlier `(3631,5755,1507)` sector is an instance. Other word orderings and all-source nonperiodic support have not been eliminated. The new theorem does not change the common numerical interval certificate or claim a cycle record.

Every admitted descent also excludes its source from being the minimum of a nonbase component, so the rules address both periodic and nonperiodic minima. Global applicability on every such possible minimum remains unproved.

## Sync status

The read-back remote branch in this continuation is still at `d0c987b12c20be5978f496ab1ef2a60018caa5e5`, with all six published PR-triggered workflows successful. The older 18-file arithmetic payload and this new payload are staged additively in the delivered patch. No new remote ref or CI status is assigned without an actual successful write and read-back. The observed interruption in the prior local combined wrapper is recorded separately from successful component runs.
