# Continuation state: arithmetic of the retained first-jet source

15 September 2026. The new payload is local and additive from the authenticated head `d0c987b12c20be5978f496ab1ef2a60018caa5e5`. Read the publication receipt for remote status; do not assign a remote commit to this file.

## Proved usable reductions

The raw integral section defect `h_p(n)=C_p-D_p*n` has exact repeated-word rank `floor((nu_2(h_p(n))-1)/A_p)`. Every actual copy reduces that depth by A_p. Its zero is an original positive return, with its actual primitive and repetitions retained. The switch equation contains the original resultant and proves why a single depth cannot be asserted to decrease across all packets.

The explicit resonant family in note (12) resets that depth without bound but has a complete strict-descent proof on its original source progression. The rise/fall argument proves strict descent on the entire domain `2^(a+2b)>3^(a+b)` for a initial ones followed by b exponents at least two. The exact positive-gap theorem uses the inspected Matveev bound and a full rational certificate; the simpler 3/2 cone has an elementary proof without it.

The whole-source peak reduction retains its original first-jet length, with both chain homotopies and finite support enlargement. The existing integral first-jet witness checker accepts the resulting boundary additions without rational division by a cycle period.

## All-length cycle constraints

The interval cover certifies original convergence through 1,166,399 and excludes every nontrivial cycle with at most 1,506 exponent-one positions at arbitrary primitive lengths. This is a workbench certificate, not a published record claim.

The established-logarithm argument additionally proves `m>226*a`, with a the maximal initial one-run at an actual cycle minimum. Exact progression parameters first give the coefficient 213. Nine already-certified original sources in the retained zero fibre raise it to 226, without new discovery starts; their 639 original witness returns are replayed. It excludes every cycle whose ones occur in one cyclic run, regardless of the number or sizes of the other exponents.

The next 1,507-position cycle sector has `(m,A)=(3631,5755)`, 1,507 ones, 2,124 twos, and odd minimum in `[1166401,1505447]`. The original minimum-run restriction and all integral forcing/valuation conditions remain attached. Other ordered placements have not been exhausted.

## Actual residual source to work on

The useful all-source target is the remaining packet-switch resonance fibres. Repetition alone is now controlled, but switches can increase depth. Retain the full integer switch identity; do not substitute a nonresonant estimate in the equal-depth layer. A new complete arithmetic descent family can attach to the existing first-jet witness at its lower endpoint.

The next structural ordering direction is cycles with at least two cyclic exponent-one runs. A proof on a gap family needs its full parameter domain, original affine numerator, positivity and actual primitive-period map. The single-run exclusion is not an exclusion of all mixed-run words.

For the aperiodic source, the original integer-support requirement remains: the nested source residues must stabilize at one positive integer. An arbitrarily long sequence of distinct finite witnesses is not that integer. No global common nilpotent clock exists. The present reductions do not yet cover every singleton.

## Reproduction and mathematical boundaries

`replay.py --predecessors --rebuild-atlas` checks all new files, both new exact runs, four unchanged predecessor executions, and regenerated atlas bytes. The finite support has 926,548 original equations, actual cycle `[1]`, and height at most 196. The transcendence theorem is externally sourced, not re-proved by its finite instantiation checker. No new Lean proof, independent external review, global Collatz proof or original counterexample is claimed.
