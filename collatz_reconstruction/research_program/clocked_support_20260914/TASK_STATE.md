# Continuation task state: clocked support

## Base and publication

Authenticated source: `KokunoYumeto/collatz-workbench`, PR #2, branch `research/stopped-cycle-cohomology-20260914`, commit `6a30f1dc23110fae0abbe3b3bd5e60072a842385`.

This is a local additive continuation. The session could read the connected repository, but its 48 discovered GitHub actions exposed reads only. No commit, ref update, PR update or new remote CI is claimed. Apply the supplied additive patch to a reviewed descendant of the source branch, preserving concurrent work. `main` and the Zeta workbench have not been changed.

## Proved and executed

The original companion's two-clock transfer map now has an explicit relative marked cyclic complex. Both polynomial homotopies, a two-sided original-word inverse and the exact arithmetic specialization `u=2/3,v=2` are written and tested. The integer forcing class is retained through the explicitly inverted powers of 3, not through an unproved equality of coefficient lattices.

Actual periodic components contribute `R/(1-W_c)`; nonperiodic components contribute the explicit increasing union of inverse original ray weights. The base component is killed by the actual fixed vertex/loop quotient. All component maps and inverses are in the note. The completed inverse column exists generally but is polynomial exactly on sources reaching 1. Scalar completion retains common finite vertex support, and its injection into the larger coefficientwise completion has an explicitly described image and quotient.

The inherited descent retractions are lifted without changing roots or original paths. A root-splice correction must be multiplied by the old path's actual terminal monomial. The missing factor in the source-7 fixture yields exactly `(1-u^4*v^3)*V_5`; the corrected map satisfies both projection compositions. Ordinary specialization recovers the old maps exactly.

The reversible database certifies every odd source at most 330749, with complete word/cell decoding and independent integer replay. The witnessed original graph has a unit cochain primitive with maximum 164 and therefore a finite clocked inverse. The old 403-rise sector is closed, and the all-length exclusion reaches 678 exponent-1 positions.

## Next exact arithmetic sector

For 679 exponent-1 positions the remaining necessary data are:

```
m=1636, A=2593
minimum in [330751,583287]
679 letters 1 and 957 letters 2
679 retained gap lengths r_i >= 0, sum r_i=957
2*4^r_i*y_(i+1) - 3^(r_i+1)*y_i = 2*3^r_i
```

The original rotation offset, word and coordinate inverse `x=y+1` remain attached. The integral block maps from the predecessor and the marked polynomial specialization both reconstruct the original equations. This sector is not certified empty and contains no claimed discovered cycle. Extending original strict-descent certificates through the complete possible minimum interval is one finite route. An obstruction for all retained gap tuples is another exact target, not an assumed theorem.

## Infinite source target

For each original positive integer n, determine polynomial membership of the exact formal column `G_n=sum u^j v^(A_j-j) V_(T^j(n))`. A finite polynomial column supplies an original boundary witness. A nonbase periodic column has its retained cycle denominator and a nonzero relative period; a nonperiodic column escapes every common finite vertex set. Do not report a capped prefix as such an infinite column with certified behavior.

A formal inverse in the larger completion is already present in the original companion; repeating its existence does not settle the polynomial-membership task. Source-dependent finite degree, not a uniform degree over all integers, is the outstanding global requirement. The first-crossing exception boxes and the original integer-support residue tower remain as in the predecessor.

## Replay

`python -B collatz_reconstruction/research_program/clocked_support_20260914/replay.py`

Preserve the fixed source manifests. Further changes belong in a separately dated continuation or must explicitly update dependent live receipts without rewriting historical assertions. Tests remain enabled under optimized Python. Local exact tests are not independent external mathematical review or proof-assistant certification.
