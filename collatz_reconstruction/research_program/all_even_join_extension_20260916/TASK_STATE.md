# Exact remaining task

The fixed interior alphabet {2,6} is no longer required by these two common-future
templates. All even e are now treated, including nonunit h_e and its exact original
ternary layer. The finite input-specific bound on e is proved.

Use the complete candidate list on actual retained roots. Record every candidate,
its exact source domain, old-mask overlap, original path peak, signed clock, and
budget decision. Default selection preserves the previous n^log_3(4) budget;
unrestricted selection has the distinct n^log_2(3) bound. A failed budget face is
not an absent arithmetic source.

The new full family at source 20241207 + 1549681956*t has a smaller-source relation,
not a certified arrival-at-1 path for every parameter. Its anchor currently reduces
to the retained root 14024703 under the displayed rules. That residual is available
for a further exact source calculation.

The crossing horizon stays 11610 odd steps. Its next record and candidate interval
remain in the preceding note; this addition does not claim to have run that scan.
Global exhaustion of remaining roots and ordinary-integer-supported noncrossing
branches is still the arithmetic task. No new positive counterexample is asserted.
