# Source and execution provenance

Date of intake: 14 September 2026.

## Readable inputs

The user supplied the previous continuation text, the intended repository name `KokunoYumeto/collatz-workbench`, the alleged predecessor note path, and claimed branch/commit details. These are user-supplied source locators, not verified remote state.

Connected Drive reads recovered an older Collatz definitions record and historical original instructions about retaining coefficients, proving exact morphisms, and constructing a working replacement after a failed map. An additional historical workbench record gave a concrete example of that methodology. These were historical records, not the current Zeta methodology section. Their raw private contents and private Drive identifiers are intentionally excluded from this repository contribution. A separate local intake record retains the precise locators.

A cached public Zeta README was readable. It exposed the older six-reader collection and did not expose the requested methodology. It was not used to assign a current commit or mathematical dependency.

Primary public records consulted were the publisher's Terras bibliographic record, Tao's current arXiv abstract/metadata, and NIST DLMF's Hurwitz/Bernoulli identities. Exact reference details and scope of inspection are in note References R1–R3.

## Unavailable current state

The installed GitHub connector could not be invoked: discovery found a GitHub plugin but it was not installed, and no GitHub tool namespace was exposed. Public page/raw-file fetches of the Collatz repository did not expose readable files. Direct container network access failed. No authenticated repository read or write succeeded.

Accordingly this contribution does not claim to resume the exact local Codex process, audit the claimed PR, validate the predecessor commit, or read the current GitHub methodology. All remote claims from the pasted handoff remain unverified here.

## New mathematical content and verification

The note's Collatz packet, cylinder, restart, image-complex, marked-count, square-root, stopping-law, and chain-map calculations were independently derived in this session. Standard stopping-time ideas and Hurwitz special values are credited rather than represented as a priority claim.

`verify.py` and `verification.json` are newly generated local files. The execution receipt identifies their hashes and replays. The arithmetic checker uses exact integers and fractions, not floating-point tolerances. Infinite statements are supported by the written proofs, not a numerical extrapolation. No external review or Lean/CI run is claimed.

No existing repository file was edited. No branch, commit, pull request, remote publication, permission change, or sharing change was made. The local patch contains new files only, under the intended Collatz contribution path. No Zeta/Split-Zero repository files were written.
