# Continuation state: retain the entire space sent to supported zero

The method is Split-Zero cohomology. The new source map is the actual inclusion of the original two-clock Collatz complex into its coefficientwise clock completion. Its target contracts. The complete source module of killed representatives, modulo only original boundaries, is recovered as the kernel on completed primitives modulo finite polynomial primitives. The explicit maps and inverses are in Theorem 1.

Theorem 2 computes that same source through the direct limit of the original S-action. It replaces any finite polynomial forcing by a specific finite terminal vector, with the original boundary difference retained. Theorem 3 gives the complete finite source-kernel space, not just a supported-zero flag, and supplies both inverse decomposition maps with the original clock powers intact.

Section 7.1 transports the scalar cyclic defect to the original integral forcing through the rational subcomplex containing that entire defect. Arbitrary formal-series evaluation in the same target is obstructed by a specified unit. The successful rational restriction, full position homotopy, arithmetic coefficient map, and original prime-power obstruction remain recorded together.

## Next original-source calculation

Work on the increasing actual source submodules `ker S^N`. Extend coverage by complete arithmetic source families, not by a claim that all finite clock quotients being zero implies the original class is zero. Retain the exact terminal vector and its source carrier at every step. A cancellation relation between multiple sources is not a singleton convergence witness.

For a singleton n, the completed primitive is a finite polynomial exactly when its original orbit reaches 1. The all-one original sources `2^(N+1)-1` prove that no uniform nilpotent exponent for S exists. The required source-by-source termination is local nilpotence, not a common global clock cutoff. The canonical source intersection excludes the single infinite all-one positive itinerary despite each finite prefix being present.

The arithmetic next step is to join a proved source-family kernel calculation to the preserved descent atlas and residual splicing maps, or construct an actual surviving singleton class with a valid infinite certificate. The named negative-forcing cycle and abstract ray remain controls only. A nonzero residual at a finite cutoff is not a divergence verdict.

## Publication and ownership

The inspected parent is `ce8f784da8fb48aa19065c036cd815c05628cec4`. Its anchored-period continuation and earlier 47 contribution files are not replaced. The older local clocked-support package is separately identified; its database and numeric bound are not assigned to this branch by this increment. Current publication and CI states belong to the observed PR/issue receipt rather than a predicted result in this file.

This is persistent mathematical task state, not a scheduled background researcher. It records no new global convergence result or positive counterexample.
