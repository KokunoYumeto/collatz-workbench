# Continue on the canonical residue atoms and the ordinary source of the infinite frontier

The new result has no P/Q-language restriction: all contracting words have
at most one non-descending positive source in the original cylinder. The
stronger threshold is `n < 2^(A-a_last+1)`. Across every entire unbounded
next-exponent first-crossing family, this leaves at most the canonical
source of the OLD prefix; its next exponent is unique and exactly computed.

Use these proved all-word rules before developing another restricted
packet family. A single exact canonical test plus two linear margins
provides a complete certificate for its infinite crossing progression.
The finite low-exponent children retain their own original word, source
lattice and target map. Do not count removal of a translated family as
removal of its canonical point unless its individual inequality is checked.

The exhaustive elementary audit discharges every first crossing through
18 odd returns, on an unbounded source and unbounded last exponent. Its
1,166,058 nodes are an explicitly regenerated finite proof domain. This is
not the claim that every source crosses by 18; source 27 first crosses at
37, and `2^(N+1)-1` has at least N noncrossing odd-return prefixes.

Remaining source families: (1) canonical points at later first coefficient
crossings; (2) original positive integers realizing an infinite noncrossing
prefix chain. Empty intersection with ordinary positive integers has not
been proved for (2). Nonempty dyadic cylinders alone are not a common
ordinary-integer source. Retain the increasing residue tower and its exact
integer-intersection condition. A possible minimum of a nonbase component
must be in one of these two retained families. Nonperiodic components are
not removed by excluding finite cycles.

An accepted descent produces the original first-jet relation
`epsilon V_n = epsilon V_y` with y<n and the literal finite edge chain.
Splice the latter into an original integral endpoint witness when one is
available. Never divide by a period or interpret a rationalized zero as an
integral singleton certificate. The fixed loop at 1 is retained throughout.

The affine multiplier alone does not always prove endpoint descent. The
canonical source 165 and the recorded 17-letter word end at 167 with a
contracting coefficient; it had already descended earlier. Its translated
cylinder does descend. This falsifies an unsafe universal shortcut and
supplies the correct retained exception and repaired complete family.

Theorem 1's all-length proof uses the stated established Matveev theorem.
The finite rational enclosures and 127 rows are fully regenerated. The
finite crossing audit independently checks its own affine margins, so it
has no unresolved transcendence estimate in its finite acceptance kernel.
