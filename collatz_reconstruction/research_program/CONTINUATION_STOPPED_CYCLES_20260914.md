# Stopped transport → cycle-relative cohomology

The new entry point is [cycle-relative cohomology](cycle_relative_cohomology_20260914/README.md). The entire [preceding stopped-affine package](stopped_affine_transport_20260914/README.md) is preserved byte-for-byte.

The proofs connect the same original source/image lattice to the return obstruction, attach actual integral cycles to the relative orbit graph, and construct counterexample-preserving descent retractions from the unchanged observer. The nested cutoff maps and their finite-support pointwise limit retain the exact unresolved source.

The replay runs both contributions' ordinary and optimized Python checks and compares their bytes with the retained result files. The separate workflow contains no publication, deployment, secret access, or write permission.

At creation this is a local integration payload, not a verified current remote checkout. The existing workbench's current instructions and conventions require authenticated review before application. The mathematical status and unresolved source calculation are in [TASK_STATE.md](cycle_relative_cohomology_20260914/TASK_STATE.md). No original positive counterexample or all-integer convergence proof is claimed.
